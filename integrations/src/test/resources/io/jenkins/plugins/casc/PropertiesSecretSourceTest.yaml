credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: SYSTEM
              id: test
              username: ${testuser}
              password: ${testpassword}