credentials:
  system:
    domainCredentials:
      # global credentials
      - credentials:
        - file:
            id: secret-file
            scope: SYSTEM
            description: "Some secret file"
            fileName: my-secret-file
            # FOO_BAR base64 encoded
            secretBytes: Rk9PX0JBUg==

