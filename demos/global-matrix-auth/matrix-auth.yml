jenkins:
  authorizationStrategy:
    globalMatrix:
      grantedPermissions:
        - name: "anonymous"
          permissions:
            - "Overall/Read"
        - name: "authenticated"
          permissions:
           - "Overall/Administer"
