jenkins:
  proxy:
    name: "${PROXY_HOST}"
    port: ${PROXY_PORT}
    userName: "${PROXY_USER}"
    secretPassword: "${PROXY_PASSWORD}"
    noProxyHost: "${PROXY_NOPROXY}"
    testUrl: "${PROXY_TEST_URL}"
